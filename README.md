to clone this repo along with all submodule :

```bash
git clone --recursive https://github.com/yourusername/combined-projects.git 
```
 update submodule

```bash
git submodule update --remote
git add .
git commit -m "msg"
git push

```
